准备图标（可选）：如果需要修改图标，将图片命名为 icon.jpg 放在项目同目录下。
启动程序：
bash
运行
python tts_robot.py
方式二：打包成 EXE 可执行文件
安装打包工具：
bash
运行
pip install pyinstaller
打包命令：
bash
运行
pyinstaller -F -w -i icon.ico tts_robot.py
注：如果没有 icon.ico，可去掉 -i icon.ico 参数。